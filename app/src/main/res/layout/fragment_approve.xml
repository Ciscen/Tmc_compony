<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.auvgo.tmc.approve.fragment.ApproveOrderListFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="#f4f4f4"
        android:orientation="horizontal"
        android:visibility="gone">

        <EditText
            android:id="@+id/approve_et"
            android:layout_width="0dp"
            android:layout_height="35dp"
            android:layout_marginBottom="10dp"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="15dp"
            android:layout_marginTop="10dp"
            android:layout_weight="1"
            android:background="@drawable/shape_rectangle_border"
            android:drawableLeft="@mipmap/search"
            android:drawablePadding="5dp"
            android:hint="编号/名称"
            android:paddingLeft="10dp"
            android:textSize="14sp" />

    </LinearLayout>

    <com.auvgo.tmc.views.RefreshListView
        android:id="@+id/approve_lv"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.auvgo.tmc.views.EmptyView
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:visibility="gone" />
</LinearLayout>
